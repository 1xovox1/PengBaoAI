# 调试空白页面问题

如果页面是空白的，请按以下步骤检查：

## 1. 打开浏览器开发者工具
- 按 F12 或右键点击页面选择"检查"
- 查看 Console（控制台）标签页，看是否有红色错误信息

## 2. 检查网络请求
- 在开发者工具的 Network（网络）标签页
- 刷新页面，查看是否有文件加载失败（红色）

## 3. 常见问题

### 问题1：控制台显示模块加载错误
**解决方案：**
- 确保所有依赖已安装：运行 `npm install`
- 检查 node_modules 文件夹是否存在

### 问题2：路由404错误
**解决方案：**
- 确保访问的是 `http://localhost:3000/` 而不是其他端口
- 检查 vite.config.js 中的端口配置

### 问题3：样式未加载
**解决方案：**
- 检查浏览器控制台是否有 CSS 文件加载错误
- 确保 sass 已正确安装

## 4. 快速测试

访问测试页面：`http://localhost:3000/test`

如果测试页面能正常显示，说明基础功能正常，问题可能在具体组件。

## 5. 如果仍然空白

请提供以下信息：
1. 浏览器控制台的错误信息（截图或复制）
2. Network 标签页中失败的请求
3. 使用的浏览器和版本

